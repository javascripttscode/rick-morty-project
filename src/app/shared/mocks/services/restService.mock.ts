import { of } from 'rxjs';

export const restServiceStub = {
    getAll: () => {
        of({});
    },
    getSingle: () => {
        of({});
    },
    getMultipleCharacters: () => {
        of({});
    },
};
